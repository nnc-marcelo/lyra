# Copie para relay/.env e preencha. relay/.env é gitignored -- nunca commitar.

REPRTOIR_EMAIL=
REPRTOIR_PASSWORD=

# Gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
# O MESMO valor vai nos secrets do Lyra Cloud como RELAY_TOKEN.
RELAY_TOKEN=
